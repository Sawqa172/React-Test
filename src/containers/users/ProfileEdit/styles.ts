import styled from 'styled-components/macro';
import Password from './Password';

const Wrapper = styled.div``;

const StyledPassword = styled(Password)`
  margin-top: 30px;
`;

export { Wrapper, StyledPassword as Password };
